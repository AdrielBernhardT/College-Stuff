#include <stdio.h>

int main (){
    int angka;
    scanf("%d", &angka);
    printf("%d plus %d is %d\n", angka, angka, angka+angka);
    printf("minus one is %d\n", (angka+angka)-1);
    return 0;
}